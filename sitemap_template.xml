<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {% for post in posts %}
  <url>
    <loc>{{ post.permalink | uriencode }}</loc>
    {% if post.updated %}
    <lastmod>{{ post.updated.toISOString() }}</lastmod>
    {% elif post.date %}
    <lastmod>{{ post.date.toISOString() }}</lastmod>
    {% endif %}
    {% if post.translation_key %}
    {% for alt in posts %}
    {% if alt.translation_key == post.translation_key %}
    <xhtml:link rel="alternate" hreflang="{{ 'ja' if alt.lang == 'jp' else alt.lang }}" href="{{ alt.permalink | uriencode }}"/>
    {% if alt.lang == 'zh' %}
    <xhtml:link rel="alternate" hreflang="x-default" href="{{ alt.permalink | uriencode }}"/>
    {% endif %}
    {% endif %}
    {% endfor %}
    {% endif %}
  </url>
  {% endfor %}
</urlset>
